<div class="bg-white p-4 rounded-lg shadow-lg hover:shadow-xl transition duration-300 ease-in-out">
  <div>
    <h3 class="text-xl font-medium text-gray-800 mb-2"><%= advertisement.title %></h3>
    <p class="text-lg text-gray-600">Prix: <%= advertisement.car_product.price %> €</p>
  </div>
  <div class="mt-4 flex justify-end">
    <a href="<%= advertisement_path(advertisement.id) %>" class="text-indigo-600 hover:text-indigo-800">Voir plus</a>
  </div>
</div>
