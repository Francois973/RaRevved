<h1 class="text-2xl font-semibold mb-4">Annonces</h1>
<ul id="feed-advertisements" class="grid grid-cols-1 gap-4">
  <% @advertisements.each do |ad| %>
    <li class="bg-white p-4 rounded-lg shadow-lg hover:shadow-xl transition duration-300 ease-in-out">
      <div class="flex flex-col justify-between h-full">
        <div>
          <h3 class="text-xl font-medium text-gray-800 mb-2"><%= ad.title %></h3>
          <p class="text-lg text-gray-600">Prix: <%= ad.car_product.price %> €</p>
        </div>
        <div class="mt-4 flex justify-end">
          <a href="<%= "/advertissements/#{ad.id}" %>" class="text-indigo-600 hover:text-indigo-800">Voir plus</a>
        </div>
      </div>
    </li>
  <% end %>
</ul>
